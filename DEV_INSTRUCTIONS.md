### Backend
- Activate virtual environment (source /path/to/venv
- Run with `ENV=dev python manage runserver`

### Frontend
- Run with `npm run dev`

### Nginx
- Run with `nginx -c /full/path/to/nginx/nginx.conf -p /full/path/to/nginx/
- Monitor with `trail -f /full/path/to/nginx/logs/access.log`
